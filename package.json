{
  "name": "@sr/rust-wasm-demo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "description": "An example showing how to integrate Rust into a web mono-repo.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/HarryGifford/rust-wasm-demo.git"
  },
  "scripts": {
    "build": "node ./bin/build.mjs build",
    "clean": "node ./bin/build.mjs clean",
    "preinstall": "npx -y only-allow pnpm"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.4.3"
  },
  "packageManager": "pnpm"
}
